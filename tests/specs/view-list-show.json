[
  {
    "name": "View list shows empty state",
    "steps": [
      {
        "send": { "callback": "view_list:show" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No coins in your watchlist yet — tap ➕ Add coin to add one."
            }
          }
        ]
      }
    ]
  }
]
